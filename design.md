# 多 Agent 协作研究写作方案设计 v3.0

> 借鉴多 Agent 写作方法论 + Deep Research + psantanna 学术工作流工程实践，
> 设计一套**模拟人类研究员做研究、写文章**的完整工作流。
>
> **版本演进**:
> - v1.0: 六阶段多 Agent 流水线基础架构
> - v2.0: + CLAUDE.md 配置 / Agent Prompt 模板 / Deep Research / 来源分级 / 质量门禁 / 防幻觉
> - v3.0: + 编排器自动驾驶 / Plan-first 模式 / 对抗式 QA / MEMORY.md 跨会话学习 /
>         80/90/95 评分体系 / Contractor 模式 / 需求规格书 / 首次会话引导

---

## 一、参考资料核心做法总结

### 1.1 多 Agent 流水线写作

选题 Agent → 大纲 Agent → 搜索 Agent → 提炼 Agent → 写作 Agent → 审稿 Agent

关键特点：角色分离、链式传递、并行加速、质量闭环。

### 1.2 Deep Research 方法论

不是简单搜索后总结，而是模拟人类研究员的深度研究过程：
子问题分解 → 迭代搜索 → 来源评估 → 交叉验证 → 观点图谱

### 1.3 psantanna 学术工作流（v3.0 核心参考）

来自 [pedrohcgs/claude-code-my-workflow](https://github.com/pedrohcgs/claude-code-my-workflow)，
从 Emory 大学 PhD 课程（6 讲、800+ 页幻灯片）的实际生产中提炼出的工程实践：

1. **Plan-first 工作流**：非平凡任务先进 Plan Mode 出规格书，再执行
2. **编排器协议 (Orchestrator)**：Plan 批准后自动接管——实现→验证→审查→修复→评分，循环至达标
3. **对抗式 QA**：Critic Agent 与 Fixer Agent 对抗循环，最多 5 轮直到 APPROVED
4. **MEMORY.md + [LEARN] 标签**：跨会话持续学习，错误纠正持久化
5. **80/90/95 评分体系**：80 分可提交，90 分可发布，95 分为卓越
6. **Contractor 模式**：批准 Plan 后全自主执行，只在有歧义时回来请示
7. **需求规格书 (MUST/SHOULD/MAY)**：动手前先澄清需求优先级和确定性
8. **验证协议**：每步产出必须验证后才能报告完成，不许说"搞定了"却没检查

---

## 二、CLAUDE.md 配置文件 v3.0（开箱即用）

> 将以下内容保存为项目根目录的 `CLAUDE.md`。

```markdown
# 研究写作工作流 v3.0

## 角色
你是一位资深研究员兼撰稿人，具备自主规划和执行能力。

## 核心原则
- **Plan first** — 非平凡任务先进 plan mode，产出需求规格书，获批后再动手
- **Verify after** — 每步产出写入文件后必须 Read 回来确认内容正确完整
- **质量门禁** — 80 分以下不可提交，所有产出必须通过对应的质量检查
- **[LEARN] 持续学习** — 被用户纠正时，将 `[LEARN:类别] 错误做法 → 正确做法` 追加到 MEMORY.md
- **素材锁定** — 写作时只使用素材包中的数据和观点，绝不编造

## 工作模式

### 触发条件
当用户消息含"写一篇""帮我写""研究写作""深度分析"等词时，进入研究写作模式。

### 模式一：Plan-first（默认）
1. 向用户提 3-5 个澄清问题（AskUserQuestion）
2. 基于回答，创建**需求规格书**写入 `output/00-spec.md`，格式：
   - MUST: 必须满足的硬性要求
   - SHOULD: 应当满足的期望
   - MAY: 可选的加分项
   - 每条标注确定性: CLEAR / ASSUMED / BLOCKED
3. 用户批准 spec 后，进入六阶段流水线

### 模式二：Contractor（Plan 批准后自动激活）
- 按规格书和大纲全自主执行
- 只在以下情况回来请示用户：
  - 遇到 BLOCKED 项需要决策
  - 素材严重不足需要调整方向
  - 质量评分连续两轮 < 70
- 其余时间自主推进，每阶段完成后简短汇报进度

## 六阶段流水线

### 阶段一：选题深化
1. WebSearch 搜索 3-5 次了解领域现状
2. 分解为 5-8 个子问题（按"是什么→为什么→怎么样→会怎样"递进）
3. 产出 → `output/01-选题分析.md`

### 阶段二：框架设计
1. 设计文章结构，每章含标题、论证目标、所需素材、搜索词、字数
2. 产出 → `output/02-大纲.md`
3. AskUserQuestion 请用户确认大纲

### 阶段三：深度研究（并行）
1. 为每章启动 Task 子 Agent 并行搜索
2. 每个子 Agent 执行**搜索-评估-深化循环**（至少 2 轮）
3. 中英文混搜，标注来源可信度 A/B/C/D
4. 每个子 Agent 产出 → `output/03-素材/素材-第N章.md`

### 阶段四：观点提炼
1. Read 所有素材，构建各章论证链
2. 检查逻辑一致性，识别素材缺口
3. 产出 → `output/04-论证提纲.md`

### 阶段五：撰写成文
1. 逐章撰写，严格按论证提纲
2. 每个数据/观点标注来源，不可编造
3. 产出 → `output/05-article.md`

### 阶段六：审稿打磨（编排器接管）
1. 启动审稿 Task 子 Agent（逻辑+事实核查+结构+文字）
2. 如评分 < 80，启动**对抗式 QA 循环**（Critic → Fixer，最多 3 轮）
3. 审稿意见 → `output/06-审稿意见.md`
4. 修改后定稿 → `output/07-定稿.md`

## 质量评分标准 (0-100)

| 维度 | 权重 | 评分要点 |
|------|------|---------|
| 逻辑严密性 | 30% | 论点清晰、论据充分、无跳跃矛盾 |
| 事实准确性 | 25% | 数据可查证、来源可信、无编造 |
| 结构完整性 | 20% | 引言有力、过渡自然、结论呼应 |
| 语言质量 | 15% | 用词准确、句式流畅、风格统一 |
| 读者体验 | 10% | 信息密度适中、例子具体、术语有解释 |

### 质量阈值
- **80 分**: 可提交的最低标准（Commit 质量）
- **90 分**: 可发布的标准（Publication 质量）
- **95 分**: 卓越标准（Excellence）

## 产出目录结构
output/
├── 00-spec.md           ← 需求规格书
├── 01-选题分析.md
├── 02-大纲.md
├── 03-素材/
│   └── 素材-第N章.md
├── 04-论证提纲.md
├── 05-article.md
├── 06-审稿意见.md
├── 07-定稿.md
└── session-log.md       ← 会话日志

## 学习持续化
- MEMORY.md 存放跨会话学习记录
- 格式: `[LEARN:类别] 旧做法 → 新做法 — 原因`
- 每次新会话开始时 Read(MEMORY.md) 恢复学习记录
```

---

## 三、整体架构 v3.0

```
┌───────────────────────────────────────────────────────────────────────────┐
│                                                                           │
│  用户: "帮我写一篇关于 XXX 的文章"                                          │
│                                                                           │
│  ┌────────────────────────────────────┐                                   │
│  │ Plan-first 模式                     │                                   │
│  │ 1. 澄清问题 (AskUserQuestion)       │                                   │
│  │ 2. 需求规格书 (MUST/SHOULD/MAY)     │                                   │
│  │ 3. 用户批准                         │                                   │
│  └───────────────┬────────────────────┘                                   │
│                  ▼                                                        │
│  ┌────────────────────────────────────┐                                   │
│  │ Contractor 模式激活 (全自主执行)      │                                   │
│  │                                    │                                   │
│  │  阶段一: 选题深化 ──[门禁1]──→       │                                   │
│  │  阶段二: 框架设计 ──[门禁2]──→       │                                   │
│  │  阶段三: 深度研究 ──[门禁3]──→       │  ← Task 子 Agent 并行            │
│  │  阶段四: 观点提炼 ──[门禁4]──→       │                                   │
│  │  阶段五: 撰写成文 ──[门禁5]──→       │                                   │
│  │  阶段六: 审稿打磨                    │  ← 编排器接管                     │
│  │          │                          │                                   │
│  │          ▼                          │                                   │
│  │  ┌──────────────────────────┐      │                                   │
│  │  │ 编排器 (Orchestrator)     │      │                                   │
│  │  │                          │      │                                   │
│  │  │  审稿Agent → 评分         │      │                                   │
│  │  │      │                   │      │                                   │
│  │  │      ├─ ≥80 → 通过        │      │                                   │
│  │  │      │                   │      │                                   │
│  │  │      └─ <80 → 对抗式QA   │      │                                   │
│  │  │           │              │      │                                   │
│  │  │           ▼              │      │                                   │
│  │  │     Critic ←→ Fixer     │      │                                   │
│  │  │     (最多 3 轮)          │      │                                   │
│  │  │           │              │      │                                   │
│  │  │           ▼              │      │                                   │
│  │  │     重新评分 → 达标?     │      │                                   │
│  │  └──────────────────────────┘      │                                   │
│  │                                    │                                   │
│  │  定稿输出 + 更新 MEMORY.md          │                                   │
│  └────────────────────────────────────┘                                   │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 四、Plan-first + 需求规格书（v3.0 新增）

> 借鉴 psantanna 工作流的核心洞察：**AI 写作失败最常见的原因不是写得差，
> 而是写错了方向。** Plan-first 通过强制的需求澄清步骤，将方向错误消灭在动手之前。

### 4.1 需求规格书模板

```markdown
## 需求规格书: [文章标题]
生成时间: [时间戳]
状态: 待批准 / 已批准

### MUST (必须满足)
1. [需求描述] — 确定性: CLEAR
2. [需求描述] — 确定性: ASSUMED (基于 [假设])
3. [需求描述] — 确定性: BLOCKED (需要用户确认: [具体问题])

### SHOULD (应当满足)
1. [需求描述] — 确定性: CLEAR
2. ...

### MAY (可选加分)
1. [需求描述]
2. ...

### 约束条件
- 字数: [X] 字 ± 15%
- 风格: [学术/评论/科普]
- 语言: [中文/英文]
- 截止时间: [如有]

### 排除项 (明确不做)
- [不涉及的方面1]
- [不涉及的方面2]
```

### 4.2 确定性状态说明

| 状态 | 含义 | 处理方式 |
|------|------|---------|
| **CLEAR** | 用户明确表述，无歧义 | 直接执行 |
| **ASSUMED** | Claude 基于上下文推断 | 标注假设依据，用户可修正 |
| **BLOCKED** | 无法推断，需用户决策 | 必须等用户回复后才能继续 |

### 4.3 为什么需求规格书能减少返工？

```
没有 Spec:
  用户: "写篇AI教育的文章"
  Claude: (猜测方向) → 写了5000字 → 用户: "不是这个角度" → 重来

有 Spec:
  用户: "写篇AI教育的文章"
  Claude: "请确认3个问题..." → Spec(含MUST/SHOULD/BLOCKED) → 用户审批
  → 方向确认后再动手 → 返工率降低 30-50%
```

---

## 五、编排器协议（v3.0 核心新增）

> 借鉴 psantanna 的 `orchestrator-protocol.md`。
> 编排器是 Plan 批准后**自动激活**的自主执行引擎，用户无需手动调用。

### 5.1 编排器工作流

```
Plan 被用户批准
        │
        ▼
┌─ Step 1: IMPLEMENT ──────────────────────┐
│  按计划执行当前阶段                         │
│  产出写入对应文件                           │
└────────────┬─────────────────────────────┘
             ▼
┌─ Step 2: VERIFY ─────────────────────────┐
│  Read 回产出文件，确认:                     │
│  - 文件存在且非空                           │
│  - 内容完整（无截断）                        │
│  - 格式符合要求                             │
│  不许跳过验证直接报告完成                    │
└────────────┬─────────────────────────────┘
             ▼
┌─ Step 3: REVIEW ─────────────────────────┐
│  启动对应的审查 Agent:                      │
│  - 素材阶段 → 素材充分度审查                 │
│  - 写作阶段 → 全维度审稿                     │
│  产出评分 (0-100)                           │
└────────────┬─────────────────────────────┘
             ▼
┌─ Step 4: SCORE & DECIDE ─────────────────┐
│                                           │
│  ≥ 80 → 通过，进入下一阶段                  │
│  60-79 → 启动对抗式 QA 修复                 │
│  < 60 → 回退到上一阶段重做                   │
│                                           │
└────────────┬─────────────────────────────┘
             ▼
┌─ Step 5: FIX (如需) ─────────────────────┐
│  对抗式 QA 循环 (详见第六节)                 │
│  Critic → Fixer → 重新评分                  │
│  最多 3 轮                                  │
└────────────┬─────────────────────────────┘
             ▼
        评分达标？
        ├─ 是 → 进入下一阶段
        └─ 否 → 请示用户决策
```

### 5.2 验证协议（Verification）

> **核心规则：不验证就不能说"完成"。**

```
每步产出后必须执行:

1. Write(output/XX-文件.md)    ← 写入
2. Read(output/XX-文件.md)     ← 读回验证
3. 检查清单:
   ✓ 文件存在
   ✓ 内容非空且完整（无意外截断）
   ✓ 格式正确（标题层级、表格对齐等）
   ✓ 关键内容在位（如：每章都有论点、每条数据都有来源）
4. 验证通过 → 汇报 "阶段X完成，产出已验证"
   验证失败 → 修复后重新验证
```

---

## 六、对抗式 QA 循环（v3.0 核心新增）

> 借鉴 psantanna 的 Adversarial QA：两个 Agent 对抗，比单 Agent 审稿更能发现问题。

### 6.1 工作原理

```
                    ┌─────────────┐
                    │  文章初稿    │
                    └──────┬──────┘
                           ▼
              ┌───────────────────────┐
              │    Critic Agent       │
              │                       │
              │  "以最严苛的标准审查   │
              │   找出所有问题和缺陷   │
              │   不放过任何瑕疵"      │
              └───────────┬───────────┘
                          ▼
                    审查报告 (问题列表)
                          │
                          ▼
              ┌───────────────────────┐
              │    Fixer Agent        │
              │                       │
              │  "逐条修复 Critic     │
              │   指出的每个问题       │
              │   修改后说明改了什么"   │
              └───────────┬───────────┘
                          ▼
                    修复后的文章
                          │
              ┌───────────▼───────────┐
              │  Critic 再次审查       │
              │                       │
              │  全部解决 → "APPROVED" │
              │  仍有问题 → 新报告     │
              └───────────┬───────────┘
                          │
              ┌───────────▼───────────┐
              │  达标 或 达到3轮上限？  │
              │                       │
              │  是 → 输出定稿         │
              │  否 → 再循环           │
              └───────────────────────┘
```

### 6.2 Critic Agent Prompt

```
你是一位严苛的学术批评者。你的唯一目标是找出文章的每一个问题。

## 规则
- 你必须尽可能严格，不要因为"差不多就行"而放过问题
- 每个问题必须具体指出位置（章节+段落）和原因
- 问题按严重程度分类:
  - CRITICAL: 事实错误、逻辑矛盾、来源缺失
  - MAJOR: 论证不充分、结构断裂、重要遗漏
  - MINOR: 用词不当、格式问题、表述冗余

## 输出格式
### Critic 审查报告 — 第 N 轮

#### 判定: [APPROVED / NOT APPROVED]

#### CRITICAL 问题
1. [位置]: [问题描述] — 为什么这是个问题: [...]
2. ...

#### MAJOR 问题
1. [位置]: [问题描述]
2. ...

#### MINOR 问题
1. [位置]: [问题描述]
2. ...

如果所有问题都已修复，输出: "判定: APPROVED — 未发现需要修复的问题"
```

### 6.3 Fixer Agent Prompt

```
你是一位精确的文章修复专家。你会收到 Critic 的审查报告和当前文章。

## 规则
- 逐条处理 Critic 指出的每个问题
- CRITICAL 问题必须全部修复
- MAJOR 问题尽量修复
- MINOR 问题酌情处理
- 每次修改必须记录: 改了什么、为什么这样改
- 不要引入新问题（修复时不要破坏其他部分）
- 修复数据/来源问题时，使用 WebSearch 补充准确信息

## 输出格式
### 修复报告

| 问题编号 | 级别 | 修复操作 | 修改位置 |
|----------|------|---------|---------|
| C-1 | CRITICAL | [描述修改内容] | 第X章第Y段 |
| M-1 | MAJOR | [描述修改内容] | ... |
| ... | ... | ... | ... |

### 未修复项（附原因）
- [问题编号]: [原因]
```

### 6.4 为什么对抗式比单Agent审稿更有效？

| 维度 | 单 Agent 审稿 | 对抗式 QA |
|------|-------------|-----------|
| 盲点 | 自己写自己审，容易"看不见" | Critic 是全新视角，无预设 |
| 严格度 | 往往"差不多就行" | Critic 被指令"严苛到极致" |
| 修复质量 | 知道问题但可能敷衍修 | Fixer 必须逐条回应，有据可查 |
| 迭代深度 | 一轮审完就算 | 最多 3 轮，直到 APPROVED |

---

## 七、MEMORY.md 跨会话学习（v3.0 新增）

> 借鉴 psantanna 的 `[LEARN]` 标签机制。
> 人类研究员会从每次写作中积累经验，AI 也应该如此。

### 7.1 机制设计

```
┌─────────────────────────────────────────┐
│              MEMORY.md                   │
│                                          │
│  跨会话持久化的学习记录                    │
│  每次被用户纠正时自动追加                   │
│  新会话开始时自动读取                       │
│                                          │
│  格式:                                    │
│  [LEARN:来源评估]                         │
│  旧: 将知乎回答作为B级来源                 │
│  新: 知乎回答只能作为C级来源（线索用）       │
│  原因: 知乎回答未经同行评审，可信度不够      │
│                                          │
│  [LEARN:写作风格]                         │
│  旧: 使用"众所周知"等泛化表述               │
│  新: 使用"根据X机构2024年报告"等具体表述     │
│  原因: 泛化表述是幻觉的温床                 │
│                                          │
└─────────────────────────────────────────┘
```

### 7.2 MEMORY.md 模板

```markdown
# MEMORY.md — 跨会话学习记录

> 本文件由 Claude 自动维护。每次被用户纠正时追加新条目。
> 新会话开始时请先 Read 本文件。

## 学习记录

### [LEARN:来源评估] — 2026-02-16
旧: [错误做法]
新: [正确做法]
原因: [为什么]

### [LEARN:写作风格] — 2026-02-16
旧: [错误做法]
新: [正确做法]
原因: [为什么]

## 项目偏好
- 用户偏好的文体风格: [待学习]
- 用户常引用的信息源: [待学习]
- 用户关注的领域: [待学习]
```

### 7.3 学习分类标签

| 标签 | 适用场景 |
|------|---------|
| `[LEARN:来源评估]` | 来源可信度判断被纠正 |
| `[LEARN:写作风格]` | 语言/文体/表述被纠正 |
| `[LEARN:论证逻辑]` | 推理/因果/论证方式被纠正 |
| `[LEARN:领域知识]` | 专业概念/术语被纠正 |
| `[LEARN:格式规范]` | 引用格式/排版要求被纠正 |
| `[LEARN:用户偏好]` | 用户个人风格偏好 |

---

## 八、Contractor 模式（v3.0 新增）

> 借鉴 psantanna 的"批准后全自主"工作模式。

### 8.1 设计思路

```
传统模式:
  每个阶段都停下来问用户 → 频繁打断 → 效率低

Contractor 模式:
  Plan 阶段充分澄清 → 批准后全自主 → 只在异常时请示 → 效率高

比喻:
  传统模式 = 用户是"工头"，每一步都盯着
  Contractor 模式 = 用户是"甲方"，审批图纸后让施工队自己干
```

### 8.2 自主执行规则

```
Contractor 模式下，Claude 自主推进，仅在以下情况回来请示:

1. BLOCKED 项: Spec 中标记为 BLOCKED 的需求需要用户决策
2. 素材严重不足: 搜索 2 轮后仍无法找到某章的核心支撑素材
3. 质量连续低分: 对抗式 QA 3 轮后评分仍 < 70
4. 方向性冲突: 搜索到的事实与 Spec 中的假设矛盾
5. 超出范围: 写作过程中发现需要涉及 Spec 排除项中的内容

其他一切情况 → 自主处理，完成后汇报。
```

### 8.3 进度汇报格式

```
每个阶段完成后，简短汇报:

=== 阶段 X 完成 ===
产出: output/XX-文件.md
质量评分: XX/100
关键发现: [1-2 句]
下一步: 进入阶段 X+1
异常: 无 / [描述]
```

---

## 九、会话日志与上下文管理（v3.0 增强）

### 9.1 session-log.md

> 借鉴 psantanna 的 session log 和 PreCompact hook 思路。

```markdown
# Session Log — [文章标题]

## 会话元信息
- 开始时间: [...]
- 文章题目: [...]
- 当前阶段: [...]
- 整体进度: [X/6 阶段]

## 关键决策记录
| 阶段 | 决策 | 依据 | 时间 |
|------|------|------|------|
| 选题 | 聚焦K12教育 | 用户明确要求 | ... |
| 大纲 | 采用递进式结构 | 题目性质适合 | ... |
| 搜索 | 补充搜索第三章 | 素材不足 | ... |

## 阶段完成状态
- [x] 阶段一: 选题深化 (评分: 85)
- [x] 阶段二: 框架设计 (用户已确认)
- [ ] 阶段三: 深度研究 (进行中, 3/4 Agent 已完成)
- [ ] 阶段四: 观点提炼
- [ ] 阶段五: 撰写成文
- [ ] 阶段六: 审稿打磨
```

### 9.2 上下文压缩策略

```
当上下文接近窗口限制时:

1. 确保以下内容不被压缩丢失:
   - 当前阶段的任务指令
   - Spec 中的 MUST 要求
   - 最近的关键决策

2. 可安全压缩的内容:
   - 已完成阶段的中间过程
   - 子 Agent 的搜索过程详情
   - 已写入文件的产出内容（可通过 Read 恢复）

3. 压缩前自动执行:
   - 更新 session-log.md（保存当前进度和关键决策）
   - 确保所有中间产出已写入文件
```

---

## 十、首次会话引导 Prompt（v3.0 新增）

> 借鉴 psantanna 的 "first session prompt" 设计。
> 用户第一次使用时，粘贴此 Prompt 即可初始化整个工作流。

```
我已设置了多 Agent 研究写作工作流。配置文件 CLAUDE.md 已在项目中。

请执行以下初始化步骤:
1. Read(CLAUDE.md) — 理解完整工作流
2. Read(MEMORY.md) — 如果存在，恢复历史学习记录
3. 确认你理解了六阶段流水线、编排器协议、质量评分标准

然后，请用 plan-first 模式帮我写一篇文章:

题目: [你的文章题目]
要求: [字数/风格/读者/其他]

先向我提 3-5 个澄清问题，然后基于我的回答创建需求规格书。
我批准 Spec 后，进入 Contractor 模式全自主执行。
```

---

## 十一、80/90/95 评分体系详解（v3.0 新增）

### 11.1 评分维度细化

```
总分 = 逻辑(30%) + 事实(25%) + 结构(20%) + 语言(15%) + 体验(10%)

每个维度 0-100 分，加权后得出总分。

逻辑严密性 (30%):
├── 总论点是否清晰可概括 (0-25)
├── 各章论点是否递进支撑 (0-25)
├── 论据是否充分且相关 (0-25)
└── 有无逻辑矛盾或跳跃 (0-25)

事实准确性 (25%):
├── 数据是否来自素材包 (0-25)
├── 来源标注是否完整 (0-25)
├── 抽查数据是否准确 (0-25)
└── 有无编造/篡改嫌疑 (0-25)

结构完整性 (20%):
├── 引言是否有效引入 (0-25)
├── 章节间过渡是否自然 (0-25)
├── 篇幅分配是否合理 (0-25)
└── 结论是否有力呼应 (0-25)

语言质量 (15%):
├── 用词准确无歧义 (0-25)
├── 句式流畅有节奏 (0-25)
├── 风格前后统一 (0-25)
└── 无错别字语病 (0-25)

读者体验 (10%):
├── 信息密度适中 (0-25)
├── 有具体例子支撑 (0-25)
├── 术语有解释 (0-25)
└── 目标读者可理解 (0-25)
```

### 11.2 三级质量阈值的用途

```
80 分 — Commit 质量 (最低交付标准)
├── 所有 MUST 需求已满足
├── 无 CRITICAL 问题
├── 每个核心论点有至少 1 条支撑
└── 来源标注基本完整

90 分 — Publication 质量 (可发布标准)
├── 全部达到 80 分标准
├── 无 MAJOR 问题
├── 每个论点有 2+ 条不同类型的支撑
├── 包含反面观点的辩证讨论
└── 章节过渡自然流畅

95 分 — Excellence (卓越标准)
├── 全部达到 90 分标准
├── 论证有独到见解（不仅是资料堆砌）
├── 语言有感染力
├── 结构有匠心（非模板化）
└── 读后有"收获感"
```

---

## 十二、v2.0 → v3.0 改进对比

| 维度 | v2.0 | v3.0 |
|------|------|------|
| 启动方式 | 直接开始六阶段 | **Plan-first: 先出需求规格书再动手** |
| 执行模式 | 每阶段等用户确认 | **Contractor: 批准后全自主，异常才请示** |
| 审稿方式 | 单 Agent 审稿 | **对抗式 QA: Critic ↔ Fixer 循环至 APPROVED** |
| 质量评分 | 门禁通过/不通过 | **0-100 分 + 80/90/95 三级阈值** |
| 验证机制 | 无显式验证 | **验证协议: Write→Read→确认，不验证不报完** |
| 跨会话学习 | 无 | **MEMORY.md + [LEARN] 标签持久化** |
| 需求管理 | 简单问几个问题 | **MUST/SHOULD/MAY + CLEAR/ASSUMED/BLOCKED** |
| 自主修复 | 审稿后人工改 | **编排器自动: 实现→验证→审查→修复→评分 循环** |
| 会话管理 | 无 | **session-log.md + 压缩前快照** |
| 首次使用 | 无引导 | **首次会话 Prompt 一键初始化** |

---

## 十三、渐进式采用指南（v3.0 新增）

> 借鉴 psantanna 的建议：**完整系统是天花板，不是地板。从最小集开始。**

### Level 1：最小可用（5 分钟上手）

只用 CLAUDE.md 的核心流程定义，不开启编排器和对抗式 QA：

```
配置: CLAUDE.md (六阶段流程 + 质量标准)
功能: 基本的六阶段写作流水线
适合: 初次使用，快速体验
```

### Level 2：加入质量控制（推荐起步）

```
在 Level 1 基础上加入:
+ Plan-first 模式 (需求规格书)
+ 质量门禁 (阶段间检查)
+ 来源可信度评估 (A/B/C/D)
适合: 需要一定质量保证的写作
```

### Level 3：全功能（生产级）

```
在 Level 2 基础上加入:
+ 编排器自动驾驶
+ 对抗式 QA 循环
+ MEMORY.md 跨会话学习
+ Contractor 模式
+ session-log.md
适合: 高质量学术/研究写作
```

---

## 十四、Agent 角色 Prompt 模板

> 保留 v2.0 的六个角色 Prompt（选题分析师、架构师、搜索研究员、
> 思想提炼师、撰稿人、审稿编辑），v3.0 新增 Critic 和 Fixer（见第六节）。
>
> 完整 Prompt 模板见 v2.0 第四节，此处不再重复。

### Agent 角色总览

| # | Agent | 实现方式 | 核心职责 |
|---|-------|---------|---------|
| 1 | 选题分析师 | 主 Agent | 题目解读 → 子问题分解 → 范围界定 |
| 2 | 架构师 | 主 Agent | 论证结构 → 章节设计 → 逻辑链检验 |
| 3 | 搜索研究员 (×N) | Task 子 Agent (并行) | 搜索-评估-深化循环 → 素材包 |
| 4 | 思想提炼师 | 主 Agent | 素材评估 → 论点锤炼 → 论证链构建 |
| 5 | 撰稿人 | 主 Agent | 逐章写作 → 来源标注 → 风格统一 |
| 6 | 审稿编辑 | Task 子 Agent | 五维度审查 → 0-100 评分 |
| 7 | **Critic** (v3.0) | Task 子 Agent | 严苛审查 → 找出所有问题 |
| 8 | **Fixer** (v3.0) | 主 Agent | 逐条修复 → 记录修改 |

---

## 十五、总结

v3.0 在"六阶段多 Agent 流水线 + Deep Research"的基础上，融入了 psantanna 学术工作流的工程实践精华：

1. **Plan-first + 需求规格书**：写之前先澄清"写什么"，用 MUST/SHOULD/MAY 结构化需求
2. **编排器自动驾驶**：Plan 批准后自动循环 实现→验证→审查→修复→评分 至达标
3. **对抗式 QA**：Critic 与 Fixer 对抗循环，比单 Agent 审稿更严更深
4. **MEMORY.md 持续学习**：[LEARN] 标签跨会话积累，越用越懂你
5. **Contractor 模式**：批准后全自主，只在异常时请示，效率最大化
6. **80/90/95 三级评分**：量化质量标准，80 可交付、90 可发布、95 卓越
7. **验证协议**：每步 Write→Read→确认，杜绝"说搞定了但没检查"
8. **渐进式采用**：从最小集起步，按需逐步开启高级功能

**最终目标：给一个题目 → Plan-first 澄清需求 → Contractor 模式全自主执行 →
Deep Research 深度搜索 → 对抗式 QA 严格审稿 → 产出可发布的高质量文章。**

---

**参考来源:**
- [pedrohcgs/claude-code-my-workflow](https://github.com/pedrohcgs/claude-code-my-workflow) — 学术工作流模板
- [psantanna.com/claude-code-my-workflow](https://psantanna.com/claude-code-my-workflow/workflow-guide.html) — 详细指南
