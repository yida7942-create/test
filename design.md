# 多 Agent 协作研究写作方案设计

> 借鉴参考文件中"多 Agent 分工协作写文章"的思路，结合 Claude Code 的实际能力，
> 设计一套**模拟人类研究员做研究、写文章**的完整工作流。

---

## 一、参考文件核心做法总结

参考文件提出了一种**多 Agent 流水线式写作方法**，核心思路是：

> 将"写一篇文章"这个复杂任务，拆解为多个专职角色（Agent），
> 每个 Agent 负责一个阶段，前一个 Agent 的产出作为下一个 Agent 的输入，
> 像流水线一样协作完成从选题到成稿的全过程。

**参考文件的 Agent 分工**：

```
选题 Agent → 大纲 Agent → 搜索 Agent → 提炼 Agent → 写作 Agent → 审稿 Agent
```

**关键特点**：
1. **角色分离**：每个 Agent 有明确的职责边界和专属 Prompt
2. **链式传递**：上一环节的结构化产出 → 下一环节的输入
3. **并行加速**：搜索/提炼阶段可按小节并行，多个 Agent 同时工作
4. **质量闭环**：审稿 Agent 可以将修改意见反馈给写作 Agent，形成迭代

---

## 二、基于 Claude Code 的多 Agent 研究写作方案

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  用户输入: "帮我写一篇关于 XXX 的研究文章"                         │
│                                                                 │
│  ┌─────────┐                                                    │
│  │ 🎯 选题  │  Agent 1: 选题分析师                                │
│  │  分析    │  拆解题目 → 明确范围 → 提出研究问题                    │
│  └────┬────┘                                                    │
│       ▼                                                         │
│  ┌─────────┐                                                    │
│  │ 🏗️ 框架 │  Agent 2: 架构师                                    │
│  │  设计    │  定大纲 → 分章节 → 明确每节的论证目标                  │
│  └────┬────┘                                                    │
│       ▼                                                         │
│  ┌─────────────────────────────────────────┐                    │
│  │ 🔍 资料搜集 (并行)                        │                    │
│  │                                         │                    │
│  │  Agent 3a ──→ 第一章资料                  │                    │
│  │  Agent 3b ──→ 第二章资料    并行搜索       │                    │
│  │  Agent 3c ──→ 第三章资料                  │                    │
│  │  Agent 3d ──→ 数据/案例                   │                    │
│  └────────────────┬────────────────────────┘                    │
│                   ▼                                             │
│  ┌─────────┐                                                    │
│  │ 💡 观点  │  Agent 4: 思想提炼师                                │
│  │  提炼    │  归纳论点 → 提取数据 → 构建论证链                     │
│  └────┬────┘                                                    │
│       ▼                                                         │
│  ┌─────────┐                                                    │
│  │ ✍️ 写作  │  Agent 5: 撰稿人                                   │
│  │  成文    │  按大纲逐节撰写 → 融入论据 → 统一风格                  │
│  └────┬────┘                                                    │
│       ▼                                                         │
│  ┌─────────┐                                                    │
│  │ 🔍 审稿  │  Agent 6: 审稿编辑                                  │
│  │  打磨    │  逻辑检查 → 事实核验 → 文字润色 → 反馈修改             │
│  └────┬────┘                                                    │
│       ▼                                                         │
│  最终成稿输出                                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 Claude Code 工具与 Agent 角色映射

| Agent 角色 | Claude Code 实现方式 | 核心工具 |
|-----------|---------------------|---------|
| 选题分析师 | 主对话 Claude 直接执行 | `WebSearch`、`AskUserQuestion` |
| 架构师 | 主对话 Claude 直接执行 | `Write`（输出大纲文件） |
| 资料搜集员 | **`Task` 子 Agent（并行）** | `WebSearch`、`WebFetch` |
| 思想提炼师 | 主对话 Claude 执行 | `Read`（读取搜集结果） |
| 撰稿人 | 主对话 Claude 执行 | `Write`、`Edit` |
| 审稿编辑 | **`Task` 子 Agent** | `Read`、`WebSearch`（事实核查） |

---

## 三、六阶段详细流程

### 阶段一：选题分析（Topic Analysis Agent）

**模拟人类行为**：拿到题目后，先理解题意、划定边界、提出核心研究问题

**执行方式**：主对话直接执行

**步骤**：
1. 解读用户给出的题目/主题
2. 使用 `WebSearch` 快速了解该领域的当前热点和争议
3. 使用 `AskUserQuestion` 与用户确认写作方向和侧重点
4. 输出**选题分析报告**

**产出格式**：
```markdown
## 选题分析报告

### 题目
[用户给出的题目]

### 核心研究问题
1. [问题 1]
2. [问题 2]
3. [问题 3]

### 写作范围界定
- 聚焦: [要重点讨论的方面]
- 排除: [不讨论的方面]
- 目标读者: [读者画像]

### 领域现状速览
- 主流观点: ...
- 争议焦点: ...
- 最新进展: ...

### 预期结论方向
[初步假设/预期论点]
```

---

### 阶段二：框架设计（Structure Architect Agent）

**模拟人类行为**：做研究前先定好论文框架，明确每一部分要论证什么

**执行方式**：主对话直接执行

**步骤**：
1. 基于选题分析报告，设计文章整体结构
2. 为每个章节定义：标题、论证目标、需要的数据/资料类型
3. 确定论证逻辑链（章节间的递进关系）
4. 使用 `Write` 将大纲写入文件，作为后续所有阶段的"蓝图"

**产出格式**：
```markdown
## 文章大纲

### 元信息
- 预估字数: 5000 字
- 文体风格: 学术分析 / 深度评论 / 科普
- 论证结构: 总分总 / 递进式 / 对比式

### 结构

#### 一、引言 (800字)
- **目标**: 引出问题，说明研究意义
- **需要的素材**: 背景数据、引发关注的事件
- **核心句**: [一句话概括本节论点]

#### 二、[主体第一部分标题] (1200字)
- **目标**: 论证 [观点 A]
- **需要的素材**: [具体数据类型]、[案例类型]
- **论证逻辑**: 现象描述 → 数据佐证 → 因果分析
- **核心句**: [一句话概括本节论点]

#### 三、[主体第二部分标题] (1200字)
- ...

#### 四、[主体第三部分标题] (1000字)
- ...

#### 五、结论 (800字)
- **目标**: 总结论点，给出前瞻/建议
- **需要的素材**: 前文论点汇总
```

---

### 阶段三：资料搜集（Research Agents - 并行）

**模拟人类行为**：按大纲需求，分头查找文献、数据、案例

**执行方式**：使用 `Task` 工具启动**多个子 Agent 并行搜索**

> 这是整个流程中最能体现"多 Agent 协作"的环节。
> 每个章节的素材需求不同，可以同时派出多个搜索 Agent。

**步骤**：
1. 根据大纲中每个章节的"需要的素材"，为每个章节创建一个搜索任务
2. 使用 `Task` 工具**并行**启动多个子 Agent
3. 每个子 Agent 独立执行：`WebSearch` 搜索 → `WebFetch` 读取关键页面 → 整理素材
4. 所有子 Agent 的结果汇总到素材库文件

**并行调用示例**（伪代码）：

```
# 同时启动 4 个搜索 Agent，互不等待

Task(Agent 3a):
  "为文章第二章搜集资料。
   论证目标: [观点A]
   需要的数据: [具体类型]
   请用 WebSearch 搜索相关学术观点、统计数据、案例，
   用 WebFetch 读取关键页面，整理为结构化素材。"

Task(Agent 3b):
  "为文章第三章搜集资料。
   论证目标: [观点B]
   ..."

Task(Agent 3c):
  "为文章第四章搜集资料。
   论证目标: [观点C]
   ..."

Task(Agent 3d):
  "搜集文章所需的宏观背景数据和典型案例。
   背景方向: [XXX]
   请搜索权威机构报告、统计年鉴、典型事件..."
```

**每个子 Agent 的产出格式**：
```markdown
## 第 N 章素材包

### 关键数据
1. [数据点]: [数值] — 来源: [URL/机构名]
2. ...

### 学术/权威观点
1. [人物/机构] 认为: "[引述]" — 来源: [URL]
2. ...

### 案例
1. [案例名称]: [简述] — 来源: [URL]
2. ...

### 反面观点（供辩证讨论）
1. [反面论点]: [简述] — 来源: [URL]
```

---

### 阶段四：观点提炼（Insight Extraction Agent）

**模拟人类行为**：阅读搜集到的资料，提炼核心论点，构建论证链

**执行方式**：主对话 Claude 执行

**步骤**：
1. 使用 `Read` 读取所有素材包文件
2. 对每个章节，从素材中提炼出：
   - **核心论点**（本节要证明什么）
   - **支撑论据**（用什么数据/案例/引述来证明）
   - **论证路径**（从A到B的逻辑推导链）
   - **可能的反驳及回应**
3. 检查全文论证的逻辑一致性
4. 输出**论证提纲**

**产出格式**：
```markdown
## 论证提纲

### 全文核心论点
[一句话总论点]

### 论证链路
观点A (第二章) → 支撑观点B (第三章) → 推导出观点C (第四章) → 总结论

### 各章论证设计

#### 第二章: [标题]
- **论点**: [明确陈述]
- **论据 1**: [数据] — 证明 [什么]
- **论据 2**: [案例] — 说明 [什么]
- **论据 3**: [权威引述] — 增强 [可信度]
- **预设反驳**: [反方观点] → 回应: [如何反驳]
- **本章结论句**: [承上启下的过渡]

#### 第三章: [标题]
- ...
```

---

### 阶段五：撰写成文（Writing Agent）

**模拟人类行为**：按照大纲和论证提纲，逐节撰写正文

**执行方式**：主对话 Claude 执行，使用 `Write` / `Edit` 工具

**步骤**：
1. 按章节顺序逐一撰写
2. 每节写作时参照：
   - 大纲中该节的目标和字数要求
   - 论证提纲中该节的论点和论据
   - 素材包中的具体数据和引述
3. 注意维护全文的：
   - **语气一致性**（学术/通俗/正式）
   - **逻辑连贯性**（章节间过渡自然）
   - **引用规范性**（数据和观点标注来源）
4. 使用 `Write` 输出完整文章

**写作规范**：
```
- 每段开头有明确的主题句
- 数据引用标注来源
- 章节之间有过渡段/过渡句
- 避免空泛论述，每个观点都有具体支撑
- 结论呼应引言中提出的问题
```

---

### 阶段六：审稿打磨（Review & Polish Agent）

**模拟人类行为**：写完后自己审读 + 请人审稿，修改完善

**执行方式**：使用 `Task` 启动审稿子 Agent（模拟"第二双眼睛"）

**审稿子 Agent 的检查清单**：

```markdown
## 审稿检查维度

### 1. 逻辑审查
- [ ] 各章论点是否清晰明确
- [ ] 论据是否充分支撑论点
- [ ] 章节间逻辑是否连贯递进
- [ ] 有无逻辑跳跃或自相矛盾

### 2. 事实核查
- [ ] 关键数据是否准确（用 WebSearch 抽查验证）
- [ ] 引述是否忠实原意
- [ ] 是否存在过时信息

### 3. 结构审查
- [ ] 篇幅分配是否合理
- [ ] 引言是否有效引出主题
- [ ] 结论是否有力回应研究问题

### 4. 文字审查
- [ ] 用词是否准确、无歧义
- [ ] 句式是否流畅、无冗余
- [ ] 风格是否统一
```

**审稿流程**：
1. 子 Agent 读取全文，按检查清单逐项审查
2. 使用 `WebSearch` 对关键数据进行抽样事实核查
3. 输出**审稿意见报告**
4. 主对话 Claude 根据审稿意见使用 `Edit` 修改文章
5. 如有重大问题，可进入**第二轮审稿**

---

## 四、完整执行示例

```
用户: 帮我写一篇关于"人工智能对教育行业的影响与未来趋势"的深度分析文章，
     5000字左右，面向教育从业者。

=== 阶段一：选题分析 (主 Agent) ===

[WebSearch] → 搜索 "AI 教育 影响 2024 2025 趋势"
[WebSearch] → 搜索 "人工智能教育应用 最新研究"
[AskUserQuestion] → 确认: 侧重K12还是高等教育？要不要讨论政策？
→ 产出: 选题分析报告.md

=== 阶段二：框架设计 (主 Agent) ===

→ 定出五章结构:
  一、引言: AI+教育的时代背景
  二、现状: AI 在教育中的典型应用场景
  三、影响: 对教师、学生、教学模式的深层改变
  四、挑战: 公平性、数据隐私、技术依赖
  五、趋势: 未来3-5年的发展方向
→ 产出: 大纲.md

=== 阶段三：资料搜集 (4 个子 Agent 并行) ===

Task(Agent-A): 搜集"AI教育应用场景"数据和案例
  [WebSearch] "AI adaptive learning platforms statistics"
  [WebSearch] "智能批改 AI助教 应用案例"
  [WebFetch] 读取 McKinsey/UNESCO 相关报告
  → 产出: 素材-第二章.md

Task(Agent-B): 搜集"AI对教学模式影响"研究
  [WebSearch] "AI impact on teaching methods research"
  [WebSearch] "人工智能改变课堂 教师角色转变"
  [WebFetch] 读取学术论文摘要
  → 产出: 素材-第三章.md

Task(Agent-C): 搜集"AI教育伦理挑战"观点
  [WebSearch] "AI education ethics privacy concerns"
  [WebSearch] "教育公平 数字鸿沟 AI"
  → 产出: 素材-第四章.md

Task(Agent-D): 搜集"AI教育未来趋势"预测
  [WebSearch] "future of AI in education 2025-2030"
  [WebSearch] "教育科技趋势预测 报告"
  → 产出: 素材-第五章.md

=== 阶段四：观点提炼 (主 Agent) ===

[Read] 素材-第二章.md ~ 素材-第五章.md
→ 提炼各章核心论点和论证链
→ 产出: 论证提纲.md

=== 阶段五：撰写成文 (主 Agent) ===

[Read] 大纲.md + 论证提纲.md + 所有素材文件
[Write] → 逐章撰写 → article.md (约5000字)

=== 阶段六：审稿打磨 (审稿子 Agent) ===

Task(审稿Agent):
  [Read] article.md
  [WebSearch] → 抽查文中3个关键数据的准确性
  → 产出: 审稿意见.md

[主Agent] [Read] 审稿意见.md
[主Agent] [Edit] article.md → 按意见逐条修改
→ 最终产出: article.md (定稿)
```

---

## 五、方案的关键设计决策

### 5.1 为什么要拆分成多个 Agent？

| 问题 | 单 Agent 写作 | 多 Agent 协作 |
|------|-------------|-------------|
| 资料搜集 | 串行搜索，一次只查一个方向 | **并行搜索**，同时查多个方向，速度翻倍 |
| 角色混乱 | 一边搜一边写，容易丢失焦点 | **职责分离**，搜的只搜，写的只写 |
| 质量保障 | 自己写自己审，难发现问题 | **独立审稿 Agent**，模拟"第二双眼睛" |
| 上下文管理 | 所有信息堆在一起，容易超限 | **分阶段产出文件**，上下文清晰可控 |
| 可控性 | 一口气生成，中途难调整 | **阶段性检查点**，每阶段可人工介入 |

### 5.2 哪些环节适合并行？

```
可并行的环节:
├── 阶段三: 多个章节的资料搜集 ← 最大加速点
├── 阶段三: 搜数据 vs 搜案例 vs 搜学术观点
└── 阶段六: 逻辑审查 vs 事实核查 (可用两个子 Agent)

必须串行的环节:
├── 阶段一 → 阶段二 (先有题目分析才能定大纲)
├── 阶段二 → 阶段三 (先有大纲才知道搜什么)
├── 阶段三 → 阶段四 (先有素材才能提炼观点)
├── 阶段四 → 阶段五 (先有论证链才能写作)
└── 阶段五 → 阶段六 (先有文稿才能审稿)
```

### 5.3 每个阶段的产出文件

```
project/
├── 01-选题分析报告.md      ← 阶段一产出
├── 02-大纲.md             ← 阶段二产出
├── 03-素材/               ← 阶段三产出
│   ├── 素材-引言.md
│   ├── 素材-第二章.md
│   ├── 素材-第三章.md
│   ├── 素材-第四章.md
│   └── 素材-第五章.md
├── 04-论证提纲.md          ← 阶段四产出
├── 05-article.md          ← 阶段五产出
└── 06-审稿意见.md          ← 阶段六产出
```

> 每个阶段的产出都是独立文件，既是当前阶段的交付物，也是下一阶段的输入。
> 用户可以在任何阶段暂停、审阅、修改后再继续。

---

## 六、与参考文件做法的对比

| 维度 | 参考文件 (多Agent框架) | 本方案 (Claude Code) |
|------|---------------------|---------------------|
| Agent 实现 | 独立 LLM 实例 + Prompt | `Task` 子 Agent + 主对话 |
| 并行能力 | 框架层面调度 | `Task` 工具原生并行 |
| 信息检索 | 外部 API 调用 | `WebSearch` + `WebFetch` |
| 中间产出 | 内存/数据库传递 | **文件持久化**，可读可改 |
| 人工介入 | 需额外开发交互界面 | `AskUserQuestion` 原生支持 |
| 迭代修改 | 重新运行 Pipeline | `Edit` 增量修改，保留已有内容 |
| 部署成本 | 需搭建多 Agent 框架 | **零部署**，直接在对话中执行 |

---

## 七、使用方式

用户只需一句话启动整个流程：

```
用户: 请用多 Agent 研究写作模式，帮我写一篇关于 "XXX" 的文章。
     要求: [字数/风格/目标读者/其他]
```

Claude Code 将自动：
1. 分析选题 → 2. 定大纲 → 3. 并行搜资料 → 4. 提炼观点 → 5. 撰写全文 → 6. 审稿打磨

每个阶段完成后自动推进到下一阶段，遇到需要用户决策的节点会主动询问。

---

## 八、总结

本方案将参考文件中"多 Agent 分工写文章"的核心思想，映射到 Claude Code 的实际工具能力上：

- **`Task` 子 Agent** = 参考文件中的独立 Agent 实例（搜索员、审稿员）
- **主对话 Claude** = 参考文件中的协调者 + 核心执行者（分析、规划、写作）
- **文件系统** = 参考文件中的 Agent 间通信通道（结构化中间产出）
- **`WebSearch` + `WebFetch`** = 参考文件中的知识检索能力
- **`AskUserQuestion`** = 参考文件中的人机交互节点

最终实现：**给一个题目 → 像人类研究员一样 → 定框架、找资料、提观点、写文章、审修改 → 输出成稿**。
